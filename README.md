Add Speech Technology & Transformer Model to Accent Detection
Background
The current project ("Text and speech PROJECT") is an Accent Detection system with:

ML models: SVM, Random Forest, XGBoost (on hand-crafted features)
DL models: 1D-CNN (Mel-spectrogram), Bi-LSTM (MFCC sequences)
Streamlit app with upload-based accent prediction
We will add two major features:

Speech Technology — Live microphone recording + Speech-to-Text transcription (OpenAI Whisper)
Transformer Model — Audio Spectrogram Transformer (AST) for accent classification
User Review Required
IMPORTANT

Transformer Model Choice: We'll implement a custom Audio Spectrogram Transformer (AST) that splits Mel-spectrograms into patches and applies multi-head self-attention. This runs on your existing synthetic data (no external pretrained model download needed). This keeps training fast and avoids large downloads.

IMPORTANT

Speech-to-Text Engine: We'll use OpenAI Whisper (openai-whisper package) for speech transcription. This requires ~1GB model download (base model) on first use. Alternatively, we can use whisper-tiny (~75MB) for faster but slightly less accurate results.

WARNING

Live Microphone: Streamlit's st.audio_input() widget (Streamlit ≥1.33) will be used for browser-based microphone recording. This is simpler and more reliable than streamlit-webrtc. Users click Record → speak → get accent + transcription.

Proposed Changes
1. Dependencies
[MODIFY] 
requirements.txt
Add new dependencies:

diff
+# Transformer Model
+transformers>=4.35.0
+
+# Speech-to-Text (Whisper)
+openai-whisper>=20231117
+ffmpeg-python>=0.2.0
2. Transformer Model — Training
[NEW] 
train_transformer.py
Implements the Audio Spectrogram Transformer (AST):

Architecture: Splits Mel-spectrograms into patches → Linear patch embedding → Positional encoding → Transformer Encoder (4 layers, 4 heads) → Classification head
Input: Same mel_spec.npy data used by CNN (shape: N × 128 × 94)
Patch size: 16×16 patches from the spectrogram
Training: Same train/test split, CrossEntropyLoss, AdamW optimizer, cosine annealing scheduler
Output: Saves models/dl/transformer_model.pth + metrics to results/metrics.json
Key model structure:

AudioSpectrogramTransformer
├── PatchEmbedding (Conv2d → flatten → linear projection)
├── [CLS] token + Positional Encoding
├── TransformerEncoder (4 layers × 4 heads, d_model=128)
├── LayerNorm
└── ClassificationHead (Linear → ReLU → Dropout → Linear → 8 classes)
3. Speech-to-Text Module
[NEW] 
speech_to_text.py
Implements Speech-to-Text using OpenAI Whisper:

SpeechToText class with configurable Whisper model size (tiny, base, small)
transcribe(audio_path) → returns { text, language, segments }
transcribe_array(audio_array, sr) → same but from numpy array
Auto-downloads model on first use
Caches loaded model in session for performance
4. Prediction Pipeline Updates
[MODIFY] 
predict.py
Import AudioSpectrogramTransformer from train_transformer.py
Add elif model_type_upper == 'TRANSFORMER': branch in _load_model()
Add _predict_transformer() method (reshapes mel-spec to 2D for patch embedding)
Add 'Transformer' detection in get_available_models()
5. Pipeline Runner Update
[MODIFY] 
run_pipeline.py
Add Step 4.5: Train Transformer model (between DL training and evaluation)
Update step numbering to 6 total steps
6. Streamlit App — New Speech Page
[MODIFY] 
app.py
Add new navigation option: "🗣️ Speech-to-Text"
Add page_speech_to_text() function:
Microphone recording via st.audio_input("Record your voice")
Audio processing → runs accent detection + Whisper transcription
Results display: Accent prediction card + transcribed text card
Model selection: User can choose any trained model for accent detection
Add "Transformer" to the model selector
Update the About page to mention Speech-to-Text and Transformer model
[MODIFY] 
components.py
Add render_transcription_card(text, language) — Displays transcribed text with a glass-card design
Add render_recording_status() — Animated recording indicator
7. Model Directory
[NEW] models/dl/transformer_model.pth (auto-generated by training)
